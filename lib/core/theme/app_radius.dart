import 'package:flutter/material.dart';

class AppRadius {
  static const BorderRadius inputs = BorderRadius.all(Radius.circular(8.0));
  static const BorderRadius buttons = BorderRadius.all(Radius.circular(8.0));
  static const BorderRadius cards = BorderRadius.all(Radius.circular(12.0));
  static const BorderRadius pills = BorderRadius.all(Radius.circular(999.0));
}
